meta {
  name: sendMoney
  type: http
  seq: 12
}

post {
  url: {{base_url}}/transaction
  body: json
  auth: bearer
}

headers {
  Bearer: 
}

auth:bearer {
  token: {{secret}}
}

body:json {
  {
    "amount": 10000000,
    "destinationId": {{destinationId}}
  }
}

settings {
  encodeUrl: true
}
