meta {
  name: loginUser
  type: http
  seq: 3
}

post {
  url: {{base_url}}/auth/login
  body: json
  auth: bearer
}

auth:bearer {
  token: {{secret}}
}

body:json {
  {
    "username":"user",
    "password":"passsword"
  }
}

settings {
  encodeUrl: true
}
