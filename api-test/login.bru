meta {
  name: login
  type: http
  seq: 1
}

post {
  url: {{base_url}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "username":"nursyah",
    "password":"password"
  }
}

settings {
  encodeUrl: true
}
